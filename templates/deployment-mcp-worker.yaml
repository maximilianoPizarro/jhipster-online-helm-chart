{{- if .Values.mcpWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jhipster-online.fullname" . }}-mcp-worker
  labels:
    {{- include "jhipster-online.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp-worker
spec:
  replicas: {{ .Values.mcpWorker.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "jhipster-online.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: mcp-worker
  template:
    metadata:
      labels:
        {{- include "jhipster-online.labels" . | nindent 8 }}
        app.kubernetes.io/component: mcp-worker
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "jhipster-online.serviceAccountName" . }}
      containers:
        - name: mcp-worker
          image: "{{ .Values.mcpWorker.image.repository }}:{{ .Values.mcpWorker.image.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: {{ .Values.mcpWorker.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
